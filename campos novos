id_pagseguro
tipoEntrega
tipoVenda
banco_deposito
agencia_deposito
contacorrente_deposito


ALTER TABLE `vendas_clarotv` ADD `banco_deposito` VARCHAR( 100 ) NOT NULL AFTER `conta_corrente` ,
ADD `agencia_deposito` VARCHAR( 100 ) NOT NULL AFTER `banco_deposito` ,
ADD `contacorrente_deposito` VARCHAR( 100 ) NOT NULL AFTER `agencia_deposito` 
